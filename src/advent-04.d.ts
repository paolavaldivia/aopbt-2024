export declare type Position = { x: number; y: number };
declare function fastPostOfficeFinderEmulator(initialPosition: Position, targetPosition: Position): number;
export default fastPostOfficeFinderEmulator;
